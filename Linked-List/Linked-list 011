<!DOCTYPE html>
<html>
<body>

<p id="result"></p>

<script src="main.js"></script>

</body>
</html>
//JS
let node1 = { value: 10, next: null };
let node2 = { value: 20, next: null };
let node3 = { value: 30, next: null };

node1.next = node2;
node2.next = node3;

let index = 1;

node1.next = node3;

console.log(node1);

document.getElementById("result").innerHTML = node1.value + " → " + node1.next.value;
//SELF
10 → 20 → 30
 0    1    2
OLDIN:
10 → 20 → 30

KEYIN:
10 ─────→ 30
Remove → o‘chirish
Index → joylashuv
next → keyingi Node
Node → element
