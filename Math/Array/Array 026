<!DOCTYPE html>
<html>
<body>

<h2 id="result"></h2>

<script src="main.js"></script>

</body>
</html>

//JS
function findPair(numbers, target) {
    for (let i = 0; i < numbers.length; i++) {
        for (let j = i + 1; j < numbers.length; j++) {

            if (numbers[i] + numbers[j] === target) {
                return [i, j];
            }

        }
    }
}

let numbers = [10, 20, 10, 40, 50, 60, 70];

let result = findPair(numbers, 50);

console.log(result);

document.getElementById("result").innerHTML = result;

//OZIM UCHUN
index:   0   1   2   3   4   5   6
         ↓   ↓   ↓   ↓   ↓   ↓   ↓
array:  10  20  10  40  50  60  70

// Pair = juftlik
// Target = kerakli yig‘indi
// index = elementning joylashuvi
// for = elementlarni tekshirish
// length = array uzunligi
